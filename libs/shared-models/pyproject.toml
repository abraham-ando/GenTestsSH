[project]
name = "gen-tests-shared-models"
version = "0.1.0"
description = "Shared Pydantic models for GenTestsSH"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
